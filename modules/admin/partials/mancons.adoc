[TIP]
====
Начиная с модуля {wf} версии {wf-cu5}, поддерживается два {do-what}:

ifndef::init-config[]
* Из интерфейса {of-mc} без необходимости работы с форматом `.json`: "xref:dev@mgmtconsole:servers:{link}"
* При помощи настройки в конфигурационном файле модуля, см. подробнее ниже
endif::init-config[]
ifeval::["{init-config}" == "yes"]
* Из интерфейса {of-mc} без необходимости работы с форматом `.json` см. ниже раздел {first}
* При помощи настройки в конфигурационном файле модуля, см. ниже раздел {second}
endif::[]
ifeval::["{add-console}" == "yes"]
* Из интерфейса {of-mc} без необходимости работы с форматом `.json` см. раздел "xref:dev@mgmtconsole:servers:{link}"
* При помощи настройки в конфигурационном файле модуля, см. ниже
endif::[]
ifeval::["{old-cns}" == "yes"]
* Из интерфейса {of-mc}, см. раздел "xref:dev@mgmtconsole:servers:{link}"
* При помощи программы "{cns}", см. ниже
endif::[]
ifeval::["{preparation}" == "yes"]
* Полностью из интерфейса {of-mc} без необходимости работы с форматом `.json`, минуя "{cns}": "xref:dev@mgmtconsole:servers:{link}"
* При помощи настройки в конфигурационном файле модуля и программе "{cns}", см. подробнее ниже
endif::[]
====
// Лень пояснять, тут всё очевидно, но если всё же не понятно, см. аналогичные partials в админе ВК и Платформы