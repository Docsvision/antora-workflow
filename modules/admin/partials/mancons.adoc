ifndef::installation[]
[TIP]
====
.Модуль "{wf}" поддерживает следующие {do-what}:
ifndef::init-config[]
* Из интерфейса {of-mc} без необходимости работы с форматом `.json`, начиная с версии {wf-cu-mc}: "xref:6.2@mgmtconsole:servers:{link}"
* При помощи настройки в конфигурационном файле модуля, в версиях ниже указанной, см. подробнее ниже
endif::init-config[]
ifeval::["{init-config}" == "yes"]
* Из интерфейса {of-mc} без необходимости работы с форматом `.json`, начиная с версии {wf-cu-mc}, см. ниже раздел {first}
* При помощи настройки в конфигурационном файле модуля, в версиях ниже указанной, см. ниже раздел {second}
endif::[]
ifeval::["{add-console}" == "yes"]
* Из интерфейса {of-mc} без необходимости работы с форматом `.json` см. раздел "xref:6.2@mgmtconsole:servers:{link}"
* При помощи настройки в конфигурационном файле модуля, см. ниже
endif::[]
ifeval::["{old-cns}" == "yes"]
* Из интерфейса {of-mc}, начиная с версии {wf-cu-mc}, см. раздел "xref:6.2@mgmtconsole:servers:{link}"
* При помощи программы "{cns}", в версиях ниже указанной, см. ниже
endif::[]
ifeval::["{preparation}" == "yes"]
* Полностью из интерфейса {of-mc} без необходимости работы с форматом `.json`, минуя "{cns}", начиная с версии {wf-cu-mc}: "xref:6.2@mgmtconsole:servers:{link}"
* При помощи настройки в конфигурационном файле модуля и программе "{cns}", в версиях ниже указанной, см. подробнее ниже
endif::[]
ifeval::["{mail-settings}" == "yes"]
* Из интерфейса {of-mc} без необходимости работы с форматом `.json`, в версиях ниже указанной: "xref:6.2@mgmtconsole:{link}"
* При помощи настройки в конфигурационном файле модуля, см. подробнее ниже
endif::[]
====
endif::installation[]
// Лень пояснять, тут всё очевидно, но если всё же не понятно, см. аналогичные partials в админе ВК и Платформы